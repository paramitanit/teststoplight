name: Run Spectral on Pull Requests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Run Spectral
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      # Run Spectral
      - uses: sede-open/spectral-lint-action@v1.0
        with:
          file_path: 'doc/api/*.yaml'
          spectral_ruleset: .spectral.yaml
